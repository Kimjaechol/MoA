# ─── Supabase (required) ───
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# ─── Admin ───
ADMIN_SECRET=your-admin-secret

# ─── Encryption key for API keys (AES-256-GCM) ───
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MOA_ENCRYPTION_KEY=your-64-char-hex-key

# ─── MoA public-facing URL ───
NEXT_PUBLIC_BASE_URL=https://mymoa.app
NEXT_PUBLIC_API_URL=https://mymoa.app

# ─── Railway backend URL (Kakao webhook server) ───
RAILWAY_BACKEND_URL=https://your-moa-project.up.railway.app

# ─── LLM API Keys (server env keys — used when user has no own keys, 2x credit) ───
# At least one is recommended; all are optional
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...
# XAI_API_KEY=xai-...
# MISTRAL_API_KEY=...

# ─── Telegram ───
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_WEBHOOK_SECRET=your-random-secret

# ─── Discord ───
DISCORD_APPLICATION_ID=123456789
DISCORD_PUBLIC_KEY=hex-public-key
DISCORD_BOT_TOKEN=your-bot-token

# ─── Slack ───
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_SIGNING_SECRET=your-signing-secret

# ─── LINE ───
# LINE_CHANNEL_ACCESS_TOKEN=your-channel-access-token
# LINE_CHANNEL_SECRET=your-channel-secret

# ─── WhatsApp (Meta Cloud API) ───
# WHATSAPP_TOKEN=your-permanent-access-token
# WHATSAPP_VERIFY_TOKEN=your-chosen-verify-token
# WHATSAPP_PHONE_NUMBER_ID=123456789
# WHATSAPP_APP_SECRET=your-app-secret

# ─── Async Queue (Optimization 2 — optional) ───
# QSTASH_TOKEN=your-upstash-qstash-token
# QSTASH_URL=https://qstash.upstash.io
# WORKER_SECRET=your-worker-shared-secret

# ─── Semantic Cache (Optimization 5 — optional) ───
# UPSTASH_VECTOR_REST_URL=https://your-vector.upstash.io
# UPSTASH_VECTOR_REST_TOKEN=your-vector-token
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-redis-token

# ─── Real-time Interpreter (uses Gemini 2.5 Flash by default) ───
# The interpreter API uses the LLM keys above (GEMINI_API_KEY preferred).
# Voice interpretation requires the MoA desktop or mobile app.

# ─── MoA Gateway server (optional — for 12 additional channels) ───
# The Gateway handles Signal, Matrix, MS Teams, Google Chat, Mattermost,
# Nextcloud Talk, Zalo, Twitch, Nostr, BlueBubbles, Tlon, iMessage.
# MOA_GATEWAY_URL=https://moa-gateway.fly.dev
# MOA_API_SECRET=shared-secret-for-gateway-auth
